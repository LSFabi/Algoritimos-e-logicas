#include <stdio.h>
#include <conio.h>

int main() {
    int num, soma = 0;

    while (soma < 100) {
        printf("Digite um número: ");
        scanf("%d", &num);

        soma += num;
    }

    printf("A soma dos números é igual ou maior que 100.\n");

    i++;
}

